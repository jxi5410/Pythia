requests>=2.28.0
numpy>=1.24.0
pandas>=2.0.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
fastapi>=0.110.0
uvicorn>=0.27.0
scipy>=1.11.0
# Causal inference (optional but recommended)
# pycausalimpact has pandas 2.0 compat issues — falls back to z-score if unavailable
# pip install pycausalimpact  # uncomment when pandas compat is fixed
tigramite>=5.2.0
